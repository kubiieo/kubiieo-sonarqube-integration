name: Build
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest
    steps:
      - name: checkout-code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis

      # Install Python
      - name: Setup Python Version
        uses: actions/setup-python@v2
        with:
          python-version: 3.8    

      # Install python dependencies
      - name: Install Python dependencies
        run: python3 -m pip install -r requirements.txt

      # Run PyTest Coverage Report
      #- name: Run Pytest Coverage
      #  run: pytest main.py --cov=. --cov-report=xml --cov-report=term    

      - name: SonarQube Scan
        continue-on-error: ${{ github.triggering_actor == 'genie9090' || github.triggering_actor == 'foo.bar' }}
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        # # https://community.sonarsource.com/t/sonarqube-not-reporting-to-github-for-merge-queue-event/112436
        # with:  
        #   args: >
        #     --define sonar.scm.revision="${{ github.sha }}"          
